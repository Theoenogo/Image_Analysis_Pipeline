# Reference MATLAB + ImageJ scripts

Originals, preserved for reference. Not maintained — the Python port in
the parent folder supersedes them.

Expected files:

- `H_roi_duplicate_image_all_channels_Recursive.m`
- `ROI_Select_Duplicate_TIFF_Loop.ijm`

The user also runs `Color_Merge_Automated_PreloadROIs_Adjust.ijm` in
ImageJ between `roi_drawing/` and this step. That macro is **kept as-is**
in ImageJ (it's a manual ROI-editing UI) and can also live here for
documentation.
